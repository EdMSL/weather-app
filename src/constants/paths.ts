import webpackPaths from '$webpack/paths';

export const PATHS = {
  SVG_SPRITE: webpackPaths.SVG_SPRITE,
};

export const PathName = {
  CURRENT_WEATHER: '/current',
  FIVE_DAY_FORECAST: '/five_day_forecast',
};
